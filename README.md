# Go Http Server Router

> This is a simple http server router that can be used to route requests to different handlers based on the request
> path.
>
> It's written in Go and uses only the standard library.

## Structure

```./api``` - Contains a simple example together with the ``main.go`` file

```./router``` - Contains the package for the router

## Usage

```shell
make run
```
